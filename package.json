{
  "name": "upcarz-scheduler",
  "version": "1.0.0",
  "description": "Sistema de agendamento para lavagem de carros da Upcarz",
  "main": "index.js",
  "scripts": {
    "dev": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "build": "tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "start": "live-server --open=\"./index.html\" --port=3000",
    "predeploy": "npm run build",
    "deploy": "node package-gh-pages.js",
    "postdeploy": "echo '🚀 Deployment package ready in gh-pages/ directory'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "live-server": "^1.2.2",
    "postcss": "^8.4.33",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.4.1"
  },
  "dependencies": {
    "date-fns": "^2.30.0"
  }
}
